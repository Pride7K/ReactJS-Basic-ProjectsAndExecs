export function add2ToNumber(dispatch)
{
    dispatch({type:"add2ToNumber"})
}

export function multi7ToNumber(dispatch)
{
    dispatch({type:"multi7ToNumber"})
}

export function split25ToNumber(dispatch)
{
    dispatch({type:"split25ToNumber"})
}